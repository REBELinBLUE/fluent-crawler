sudo: false

language: php

php:
  - 7.1

cache:
  directories:
    - $TRAVIS_BUILD_DIR/vendor

before_install:
  - phpenv config-rm xdebug.ini

install:
  - travis_retry composer install --no-interaction --prefer-dist --no-suggest

script:
  - make lint
  - make phpcs
  #- make phpmd
  - make phpstan
  - make coverage
#  - make phpcpd
#  - make lines

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email: false
  
addons:
  code_climate:
    repo_token: 9b4ab3158d2fd31a0099ad730783ce9589ad942d080362b130f62bfccc0cabaf
